FILE = TABLE
PRODUCT = bambi

FLAVOR = ANY
QUALIFIERS = ""

ACTION = SETUP
   envset(BAMBI,"ROCK AND ROLL")
   sourceCompileReq("${UPS_PROD_DIR}/bambi")
   envset(BAMBI,"OPERA")
   setupRequired( "rabbit v1_0" )

ACTION = CONFIGURE
   writeCompileScript("${UPS_PROD_DIR}/bambi", SETUP)

