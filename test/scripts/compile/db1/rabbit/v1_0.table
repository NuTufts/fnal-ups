FILE = TABLE
PRODUCT = rabbit

FLAVOR = ANY
QUALIFIERS = ""

ACTION = SETUP
   envset(RABBIT,"ROCK AND ROLL")
   sourceCompileReq("${UPS_PROD_DIR}/rabbit")
   envset(RABBIT,"OPERA")

ACTION = CONFIGURE
   writeCompileScript("${UPS_PROD_DIR}/rabbit", SETUP)

