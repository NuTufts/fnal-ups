FILE = TABLE
PRODUCT = fly

FLAVOR = NULL
QUALIFIERS = ""

ACTION = SETUP
   writeCompileScript(compile.out,setup,flag)
   doDefaults()
# do environment variable tests
   envset(OPERA,"AIDA FAUST")
   envunset(OPERA)
   sourceCompileReq(requiredScript)
# should not contain this line
   envset (OPERA,"")
