set path=($path bin)
rehash
#
set path=($path emacs)
rehash
#
setenv upstmp "emacs/test"
setenv upstmp "`$UPS_DIR/bin/dropit -e -S -p "'"$path"'" -i' ' -d' ' "$upstmp"`"
if ($status == 0 && "$upstmp" != "") set path=($upstmp)
rehash
unsetenv upstmp
#
setenv upstmp "emacs/bin"
setenv upstmp "`$UPS_DIR/bin/dropit -e -S -p "'"$path"'" -i' ' -d' ' "$upstmp"`"
if ($status == 0 && "$upstmp" != "") set path=($upstmp)
rehash
unsetenv upstmp
#
unsetenv path
#
