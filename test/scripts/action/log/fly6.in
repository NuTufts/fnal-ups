set path=($path bin)
rehash
#
set path=($path emacs)
rehash
#
setenv upstmp "`dropit -p "'"$path"'" -i' ' -d' ' emacs/test`"
if ($status == 0 && "$upstmp" != "") set path=($upstmp)
rehash
unsetenv upstmp
#
setenv upstmp "`dropit -p "'"$path"'" -i' ' -d' ' emacs/bin`"
if ($status == 0 && "$upstmp" != "") set path=($upstmp)
rehash
unsetenv upstmp
#
unsetenv path
#
