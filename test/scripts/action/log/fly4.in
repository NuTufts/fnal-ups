setenv TMPDIR "`$UPS_DIR/bin/dropit -e -p "'"$TMPDIR"'" -i':' -d':' /tmp`"
if ("x${TMPDIR}" == "x") unsetenv TMPDIR
#
setenv OPERA "`$UPS_DIR/bin/dropit -e -p "'"$OPERA"'" -i' ' -d' ' FIDELIO`"
if ("x${OPERA}" == "x") unsetenv OPERA
#
setenv OPERA "`$UPS_DIR/bin/dropit -e -p "'"$OPERA"'" -i' ' -d' ' MADAM_BUTTERFLY CARMEN`"
if ("x${OPERA}" == "x") unsetenv OPERA
#
unsetenv OPERA
#
setenv SYMPHONY "`$UPS_DIR/bin/dropit -e -p "'"$SYMPHONY"'" -i' ' -d';' EROICA`"
if ("x${SYMPHONY}" == "x") unsetenv SYMPHONY
#
unsetenv FLY_DIR
#
unsetenv SETUP_FLY
#
