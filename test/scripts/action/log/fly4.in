unsetenv TMPDIR
#
setenv upstmp "`dropit -p "'"$OPERA"'" -i'' -d'' FIDELIO`"
if ($status == 0 && "$upstmp" != "") setenv OPERA "$upstmp"
unsetenv upstmp
#
setenv upstmp "`dropit -p "'"$OPERA"'" -i'' -d'' MADAM_BUTTERFLY CARMEN`"
if ($status == 0 && "$upstmp" != "") setenv OPERA "$upstmp"
unsetenv upstmp
#
unsetenv OPERA
#
unsetenv FLY_DIR
#
unsetenv SETUP_FLY
#
